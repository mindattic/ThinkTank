---
name: run
description: Build and run the LLM Think Tank Blazor app. No arguments needed.
---

When invoked:

1. Run: `dotnet run --project ThinkTank.Blazor`
2. Stream the output so the user can see build progress and any errors
3. If the build fails, summarize the error and suggest a fix
